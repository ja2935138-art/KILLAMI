# 📋 KILLAMI - Lista Completa de Características con Códigos\n\n## 1. 🎫 Menú Digital Interactivo\n\n### Frontend (React Component)\n```jsx\n// src/components/Menu.jsx\nimport React, { useState } from 'react';\n\nconst Menu = () => {\n  const [selectedCategory, setSelectedCategory] = useState('all');\n  const [cart, setCart] = useState([]);\n\n  const products = [\n    { id: 1, name: 'Hamburguesa Premium', price: 15.00, category: 'comidas', image: '🍔' },\n    { id: 2, name: 'Pizza Clásica', price: 18.50, category: 'comidas', image: '🍕' },\n    { id: 3, name: 'Refresco Grande', price: 3.50, category: 'bebidas', image: '🥤' },\n    { id: 4, name: 'Papas Fritas', price: 4.00, category: 'acompañamientos', image: '🍟' },\n    { id: 5, name: 'Postre Especial', price: 8.00, category: 'postres', image: '🎂' }\n  ];\n\n  const addToCart = (product) => {\n    const existingItem = cart.find(item => item.id === product.id);\n    if (existingItem) {\n      setCart(cart.map(item =>\n        item.id === product.id\n          ? { ...item, quantity: item.quantity + 1 }\n          : item\n      ));\n    } else {\n      setCart([...cart, { ...product, quantity: 1 }]);\n    }\n  };\n\n  const filteredProducts = selectedCategory === 'all'\n    ? products\n    : products.filter(p => p.category === selectedCategory);\n\n  return (\n    <div style={{ padding: '20px', background: '#1a1a1a', color: '#f5f5f5' }}>\n      <h1 style={{ color: '#d4af37', marginBottom: '20px' }}>🍽️ Menú KILLAMI</h1>\n      \n      {/* Categorías */}\n      <div style={{ marginBottom: '30px', display: 'flex', gap: '10px', flexWrap: 'wrap' }}>\n        {['all', 'comidas', 'bebidas', 'acompañamientos', 'postres'].map(cat => (\n          <button\n            key={cat}\n            onClick={() => setSelectedCategory(cat)}\n            style={{\n              padding: '10px 20px',\n              background: selectedCategory === cat ? '#d4af37' : '#2d2d2d',\n              color: selectedCategory === cat ? '#1a1a1a' : '#d4af37',\n              border: '2px solid #d4af37',\n              borderRadius: '5px',\n              cursor: 'pointer',\n              fontWeight: 'bold'\n            }}\n          >\n            {cat.charAt(0).toUpperCase() + cat.slice(1)}\n          </button>\n        ))}\n      </div>\n\n      {/* Productos */}\n      <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill, minmax(200px, 1fr))', gap: '20px' }}>\n        {filteredProducts.map(product => (\n          <div\n            key={product.id}\n            style={{\n              background: '#2d2d2d',\n              borderLeft: '4px solid #d4af37',\n              padding: '20px',\n              borderRadius: '10px',\n              textAlign: 'center'\n            }}\n          >\n            <div style={{ fontSize: '40px', marginBottom: '10px' }}>{product.image}</div>\n            <h3 style={{ color: '#d4af37', marginBottom: '10px' }}>{product.name}</h3>\n            <p style={{ color: '#b0b0b0', marginBottom: '15px' }}>S/. {product.price.toFixed(2)}</p>\n            <button\n              onClick={() => addToCart(product)}\n              style={{\n                width: '100%',\n                padding: '10px',\n                background: 'linear-gradient(135deg, #d4af37, #e6c547)',\n                color: '#1a1a1a',\n                border: 'none',\n                borderRadius: '5px',\n                fontWeight: 'bold',\n                cursor: 'pointer'\n              }}\n            >\n              Agregar al Carrito\n            </button>\n          </div>\n        ))}\n      </div>\n\n      {/* Carrito */}\n      <div style={{ marginTop: '40px', background: '#2d2d2d', padding: '20px', borderRadius: '10px' }}>\n        <h2 style={{ color: '#d4af37', marginBottom: '15px' }}>🛒 Carrito ({cart.length})</h2>\n        {cart.map(item => (\n          <div key={item.id} style={{ display: 'flex', justifyContent: 'space-between', marginBottom: '10px', paddingBottom: '10px', borderBottom: '1px solid #d4af37' }}>\n            <span>{item.image} {item.name} x{item.quantity}</span>\n            <span style={{ color: '#d4af37' }}>S/. {(item.price * item.quantity).toFixed(2)}</span>\n          </div>\n        ))}\n        <div style={{ marginTop: '15px', textAlign: 'right' }}>\n          <strong style={{ color: '#d4af37', fontSize: '18px' }}>\n            Total: S/. {cart.reduce((total, item) => total + (item.price * item.quantity), 0).toFixed(2)}\n          </strong>\n        </div>\n      </div>\n    </div>\n  );\n};\n\nexport default Menu;\n```\n\n---\n\n## 2. 📱 Código QR Dinámico\n\n### Backend API (Node.js + Express)\n```javascript\n// backend/src/routes/qr.js\nconst express = require('express');\nconst QRCode = require('qrcode');\nconst router = express.Router();\n\n// Generar código QR\nrouter.post('/generate', async (req, res) => {\n  try {\n    const { tableId, restaurantId } = req.body;\n    \n    // URL del menú con parámetros\n    const menuUrl = `https://killami.app/menu?table=${tableId}&restaurant=${restaurantId}`;\n    \n    // Generar QR\n    const qrCode = await QRCode.toDataURL(menuUrl);\n    \n    res.json({\n      success: true,\n      qrCode,\n      url: menuUrl\n    });\n  } catch (error) {\n    res.status(500).json({ error: error.message });\n  }\n});\n\nmodule.exports = router;\n```\n\n### Instalar dependencia:\n```bash\nnpm install qrcode\n```\n\n---\n\n## 3. 💳 Sistema de Pedidos (PDV)\n\n### Modelo de Base de Datos (Prisma)\n```prisma\n// backend/prisma/schema.prisma\nmodel Order {\n  id         String   @id @default(cuid())\n  orderNumber String   @unique\n  tableId    String?\n  table      Table?   @relation(fields: [tableId], references: [id])\n  items      OrderItem[]\n  status     OrderStatus @default(PENDING)\n  total      Float    @default(0)\n  notes      String?\n  createdAt  DateTime @default(now())\n  updatedAt  DateTime @updatedAt\n  payment    Payment?\n\n  @@map(\"orders\")\n}\n\nenum OrderStatus {\n  PENDING\n  COOKING\n  READY\n  SERVED\n  PAID\n  CANCELLED\n}\n\nmodel OrderItem {\n  id        String   @id @default(cuid())\n  orderId   String\n  order     Order    @relation(fields: [orderId], references: [id], onDelete: Cascade)\n  productId String\n  product   Product  @relation(fields: [productId], references: [id])\n  quantity  Int\n  price     Float\n  notes     String?\n  createdAt DateTime @default(now())\n\n  @@map(\"order_items\")\n}\n```\n\n### API de Pedidos\n```javascript\n// backend/src/routes/orders.js\nconst express = require('express');\nconst { PrismaClient } = require('@prisma/client');\nconst router = express.Router();\nconst prisma = new PrismaClient();\n\n// Crear pedido\nrouter.post('/create', async (req, res) => {\n  try {\n    const { tableId, items, notes } = req.body;\n\n    const order = await prisma.order.create({\n      data: {\n        orderNumber: `ORD-${Date.now()}`,\n        tableId,\n        notes,\n        status: 'PENDING',\n        items: {\n          create: items.map(item => ({\n            productId: item.productId,\n            quantity: item.quantity,\n            price: item.price\n          }))\n        }\n      },\n      include: { items: true, table: true }\n    });\n\n    res.json({ success: true, order });\n  } catch (error) {\n    res.status(500).json({ error: error.message });\n  }\n});\n\n// Obtener pedidos\nrouter.get('/list', async (req, res) => {\n  try {\n    const orders = await prisma.order.findMany({\n      include: { items: true, table: true },\n      orderBy: { createdAt: 'desc' }\n    });\n    res.json({ success: true, orders });\n  } catch (error) {\n    res.status(500).json({ error: error.message });\n  }\n});\n\n// Actualizar estado\nrouter.patch('/:id/status', async (req, res) => {\n  try {\n    const { id } = req.params;\n    const { status } = req.body;\n\n    const order = await prisma.order.update({\n      where: { id },\n      data: { status }\n    });\n\n    res.json({ success: true, order });\n  } catch (error) {\n    res.status(500).json({ error: error.message });\n  }\n});\n\nmodule.exports = router;\n```\n\n---\n\n## 4. 🪑 Gestión de Mesas\n\n### Modelo\n```prisma\nmodel Table {\n  id        String   @id @default(cuid())\n  number    Int      @unique\n  capacity  Int\n  status    TableStatus @default(AVAILABLE)\n  createdAt DateTime @default(now())\n  updatedAt DateTime @updatedAt\n  orders    Order[]\n\n  @@map(\"tables\")\n}\n\nenum TableStatus {\n  AVAILABLE\n  OCCUPIED\n  RESERVED\n  DIRTY\n}\n```\n\n### API\n```javascript\n// backend/src/routes/tables.js\nrouter.get('/status', async (req, res) => {\n  try {\n    const tables = await prisma.table.findMany({\n      include: {\n        orders: {\n          where: { status: { in: ['PENDING', 'COOKING', 'READY'] } },\n          take: 1,\n          orderBy: { createdAt: 'desc' }\n        }\n      }\n    });\n    res.json({ success: true, tables });\n  } catch (error) {\n    res.status(500).json({ error: error.message });\n  }\n});\n\nrouter.patch('/:id/occupy', async (req, res) => {\n  try {\n    const { id } = req.params;\n    const table = await prisma.table.update({\n      where: { id },\n      data: { status: 'OCCUPIED' }\n    });\n    res.json({ success: true, table });\n  } catch (error) {\n    res.status(500).json({ error: error.message });\n  }\n});\n```\n\n---\n\n## 5. 💰 Sistema de Pagos\n\n### Modelo\n```prisma\nmodel Payment {\n  id        String   @id @default(cuid())\n  orderId   String   @unique\n  order     Order    @relation(fields: [orderId], references: [id], onDelete: Cascade)\n  amount    Float\n  method    PaymentMethod\n  status    PaymentStatus @default(PENDING)\n  createdAt DateTime @default(now())\n  updatedAt DateTime @updatedAt\n\n  @@map(\"payments\")\n}\n\nenum PaymentMethod {\n  CASH\n  CARD\n  TRANSFER\n  WALLET\n}\n\nenum PaymentStatus {\n  PENDING\n  COMPLETED\n  CANCELLED\n}\n```\n\n### API de Pagos\n```javascript\n// backend/src/routes/payments.js\nrouter.post('/process', async (req, res) => {\n  try {\n    const { orderId, amount, method } = req.body;\n\n    const payment = await prisma.payment.create({\n      data: {\n        orderId,\n        amount,\n        method,\n        status: 'COMPLETED'\n      }\n    });\n\n    // Actualizar estado del pedido\n    await prisma.order.update({\n      where: { id: orderId },\n      data: { status: 'PAID' }\n    });\n\n    res.json({ success: true, payment });\n  } catch (error) {\n    res.status(500).json({ error: error.message });\n  }\n});\n```\n\n---\n\n## 6. 📊 Reportes y Estadísticas\n\n### API de Reportes\n```javascript\n// backend/src/routes/reports.js\nrouter.get('/daily-sales', async (req, res) => {\n  try {\n    const today = new Date();\n    today.setHours(0, 0, 0, 0);\n\n    const sales = await prisma.payment.aggregateAsync({\n      where: {\n        createdAt: { gte: today },\n        status: 'COMPLETED'\n      },\n      _sum: { amount: true },\n      _count: true\n    });\n\n    res.json({\n      success: true,\n      totalSales: sales._sum.amount || 0,\n      orders: sales._count\n    });\n  } catch (error) {\n    res.status(500).json({ error: error.message });\n  }\n});\n\nrouter.get('/top-products', async (req, res) => {\n  try {\n    const topProducts = await prisma.orderItem.groupBy({\n      by: ['productId'],\n      _sum: { quantity: true },\n      _count: true,\n      orderBy: { _sum: { quantity: 'desc' } },\n      take: 10\n    });\n\n    res.json({ success: true, topProducts });\n  } catch (error) {\n    res.status(500).json({ error: error.message });\n  }\n});\n```\n\n---\n\n## 7. 🔐 Autenticación y Usuarios\n\n### Modelo\n```prisma\nmodel User {\n  id        String   @id @default(cuid())\n  email     String   @unique\n  password  String\n  name      String\n  role      Role     @default(MESERO)\n  active    Boolean  @default(true)\n  createdAt DateTime @default(now())\n  updatedAt DateTime @updatedAt\n\n  @@map(\"users\")\n}\n\nenum Role {\n  ADMIN\n  GERENTE\n  MESERO\n  CHEF\n  CAJA\n}\n```\n\n### API de Autenticación\n```javascript\n// backend/src/routes/auth.js\nconst bcrypt = require('bcryptjs');\nconst jwt = require('jsonwebtoken');\n\nrouter.post('/login', async (req, res) => {\n  try {\n    const { email, password } = req.body;\n\n    const user = await prisma.user.findUnique({ where: { email } });\n    if (!user) return res.status(401).json({ error: 'Usuario no encontrado' });\n\n    const validPassword = await bcrypt.compare(password, user.password);\n    if (!validPassword) return res.status(401).json({ error: 'Contraseña incorrecta' });\n\n    const token = jwt.sign(\n      { userId: user.id, role: user.role },\n      process.env.JWT_SECRET,\n      { expiresIn: '24h' }\n    );\n\n    res.json({ success: true, token, user });\n  } catch (error) {\n    res.status(500).json({ error: error.message });\n  }\n});\n```\n\n---\n\n## 8. ⚡ Socket.io - Comunicación en Tiempo Real\n\n### Backend\n```javascript\n// backend/src/sockets.js\nconst { Server } = require('socket.io');\n\nmodule.exports = (httpServer) => {\n  const io = new Server(httpServer, {\n    cors: { origin: process.env.CLIENT_URL }\n  });\n\n  io.on('connection', (socket) => {\n    console.log('Cliente conectado:', socket.id);\n\n    // Nuevo pedido\n    socket.on('new-order', (order) => {\n      io.emit('order-created', order);\n    });\n\n    // Actualizar estado\n    socket.on('order-status-change', (data) => {\n      io.emit('order-updated', data);\n    });\n\n    // Mesa ocupada/disponible\n    socket.on('table-status-change', (data) => {\n      io.emit('table-updated', data);\n    });\n\n    socket.on('disconnect', () => {\n      console.log('Cliente desconectado');\n    });\n  });\n\n  return io;\n};\n```\n\n---\n\n## 9. 🖨️ Impresión de Comandas\n\n### API\n```javascript\n// backend/src/routes/print.js\nconst escpos = require('escpos');\nconst { USB } = require('escpos');\n\nrouter.post('/print-order', async (req, res) => {\n  try {\n    const { orderId } = req.body;\n    const order = await prisma.order.findUnique({\n      where: { id: orderId },\n      include: { items: { include: { product: true } }, table: true }\n    });\n\n    // Conectar a impresora\n    const device = new USB();\n    const printer = new escpos.Printer(device);\n\n    device.open(() => {\n      printer\n        .align('ct')\n        .text('KILLAMI POS')\n        .text('---')\n        .align('lt')\n        .text(`Pedido: ${order.orderNumber}`)\n        .text(`Mesa: ${order.table?.number || 'Mostrador'}`)\n        .text('---');\n\n      order.items.forEach(item => {\n        printer.text(`${item.product.name} x${item.quantity}`);\n      });\n\n      printer\n        .text('---')\n        .align('ct')\n        .text(new Date().toLocaleTimeString())\n        .cut()\n        .close();\n\n      res.json({ success: true, message: 'Comanda impresa' });\n    });\n  } catch (error) {\n    res.status(500).json({ error: error.message });\n  }\n});\n```\n\n---\n\n## 10. 📦 Gestión de Productos e Inventario\n\n### Modelo\n```prisma\nmodel Product {\n  id          String   @id @default(cuid())\n  name        String\n  description String?\n  price       Float\n  category    String\n  image       String?\n  active      Boolean  @default(true)\n  stock       Int      @default(0)\n  minStock    Int      @default(5)\n  createdAt   DateTime @default(now())\n  updatedAt   DateTime @updatedAt\n\n  orderItems OrderItem[]\n\n  @@map(\"products\")\n}\n```\n\n### API\n```javascript\n// backend/src/routes/products.js\nrouter.get('/list', async (req, res) => {\n  try {\n    const products = await prisma.product.findMany({\n      where: { active: true }\n    });\n    res.json({ success: true, products });\n  } catch (error) {\n    res.status(500).json({ error: error.message });\n  }\n});\n\nrouter.patch('/:id/stock', async (req, res) => {\n  try {\n    const { id } = req.params;\n    const { quantity } = req.body;\n\n    const product = await prisma.product.update({\n      where: { id },\n      data: { stock: { increment: quantity } }\n    });\n\n    res.json({ success: true, product });\n  } catch (error) {\n    res.status(500).json({ error: error.message });\n  }\n});\n```\n\n---\n\n## 📦 Dependencias Recomendadas\n\n```json\n{\n  \"dependencies\": {\n    \"express\": \"^4.18.2\",\n    \"@prisma/client\": \"^5.0.0\",\n    \"jsonwebtoken\": \"^9.1.0\",\n    \"bcryptjs\": \"^2.4.3\",\n    \"socket.io\": \"^4.7.1\",\n    \"qrcode\": \"^1.5.3\",\n    \"escpos\": \"^3.0.0\",\n    \"cors\": \"^2.8.5\",\n    \"dotenv\": \"^16.3.1\"\n  }\n}\n```\n\n---\n\n## 🚀 Próximos Pasos\n\n1. Implementar autenticación JWT\n2. Integrar pagos reales (Stripe, Niubiz)\n3. App móvil React Native\n4. Integración WhatsApp\n5. Sistema de delivery\n\n**¿Necesitas ayuda con algún módulo específico?** 🎯\n"